# gk-projectgoal-keeper-backend
goal-keeper-backend application

Run Application:

git clone repo
mvn clean install
ga naar folder root via terminal
mvn spring-boot:run
Swagger API docs voor endpoints: http://localhost:8080/swagger-ui/